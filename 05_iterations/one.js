// Terminal - node 05_iterations/one.js

//    For   Loop

for (let index = 0; index < 10; index++) {
    const element = index;
    console.log(element);
}

// console.log(index);  //  => ReferenceError: array is not defined
// Block Scope. 'index' Variable can not be accessed Outside the For Loop.




















